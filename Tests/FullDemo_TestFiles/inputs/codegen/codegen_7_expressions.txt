program {
	
	int a;
	

	a = 32 + 32;
	a = 32 - 32;
	a = 32 * 32;
	a = 32 / 32;
	a = 32 < 32;
	a = 32 > 32;
	a = 32 <= 32;
	a = 32 >= 32;
	a = 32 == 32;
	a = 32 <> 32;



	int x;
	int y;
	int z;

	x = 10 + 10; // 20
	y = 20 * 13 / 5 -2 + 4; // 54
	z = x * x + y * y; // 3316
	int c;
	x = x + y + 2 -z; // -3240

	int q;

	q = -32 + -32; // -64
	q = -x - -y; // 34

	int b1;

	b1 = x < y; // true
	b1 = x > y; // false
	b1 = x <= y; // true
	b1 = x >= y; // false
	b1 = x == y; // false
	b1 = x <> y; // true

};
