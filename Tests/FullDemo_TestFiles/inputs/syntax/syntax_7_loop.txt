program {
	for (id id = num; id < id ; id = id + id) {

	};

	for (id id = num; id < id ; id = id + id)
	
	;

	for (id id = num; id < id ; id = id + id) {
		for (id id = num; id < id ; id = id + id) {

		};
	};

	for (id id = num; id < id ; id = id + id)
		for (id id = num; id < id ; id = id + id)
		
		;
	;
};